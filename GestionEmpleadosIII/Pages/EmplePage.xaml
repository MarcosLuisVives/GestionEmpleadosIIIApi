<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestionEmpleadosIII.Pages.EmplePage"
             Title="Empleados">

    <ContentPage.ToolbarItems >
        <ToolbarItem Text="Añadir" 
                     Command="{Binding IrACrearCommand}" 
                     />
    </ContentPage.ToolbarItems>

    <Grid>
        <CollectionView ItemsSource="{Binding Empleados}"
                        SelectedItem="{Binding EmpleadoSeleccionado}"
                        SelectionMode="Single"
                        ItemTemplate="{StaticResource CardEmple}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="2" />
            </CollectionView.ItemsLayout>
        </CollectionView>
    </Grid>
</ContentPage>